# Server
PORT=5000

# Where booking/contact notifications are sent
ADMIN_EMAIL=amanvg1311@gmail.com

# SMTP credentials used to send emails.
# Easiest option: a Gmail account with a 16-character "App Password"
# (Google Account -> Security -> 2-Step Verification -> App passwords).
# You can also use any SMTP provider (SendGrid, Mailgun, Zoho, etc).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=amanvg1311@gmail.com
SMTP_PASS=wkci ceyf bwes many

# Comma-separated list of origins allowed to call this API
CORS_ORIGIN=http://localhost:5173
